import { MenubarController } from './menubar.controller';

import './menubar.component.scss';
import templateUrl from './menubar.template.html';

export const MenubarName = 'appMenubar';

export const MenubarComponent = {
    controller: MenubarController,
    templateUrl,
};
